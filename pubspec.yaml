name: watchtower_sdk
description: Watchtower Flutter SDK
version: 0.4.5
# homepage:
publish_to: none

environment:
  sdk: ^3.8.0
  flutter: '>=3.32.0'

dependencies:
  flutter:
    sdk: flutter
  grpc: ^4.3.1
  logger: ^1.3.0
  shared_preferences: ^2.5.3
  device_info_plus: ^12.2.0
  encrypt: ^5.0.1
  package_info_plus: ^4.2.0
  image: ^4.5.4
  fixnum: ^1.1.0
  crypto: ^3.0.2

  extended_shared_preferences:
    git:
      url: https://github.com/KrasnovVitaliy/extended_shared_preferences.git
      ref: main # branch name

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  import_sorter: ^4.6.0
  cider: ^0.1.4

flutter:
  plugin:
    platforms:
      android:
        package: com.kitegroup.watchtower
        pluginClass: WatchtowerPlugin
      ios:
        pluginClass: WatchtowerPlugin
